# Rise Social Studio context

Start with `CHATGPT_PROJECT_INSTRUCTIONS.md`, then read `AGENTS.md`,
`docs/EDITORIAL_RULES_SK.md`, `docs/WORKFLOW.md`, `brand/voice.sk.json` and the
single relevant skill under `.agents/skills/`.

Task state belongs to JetBrains YouTrack project `RISE`. GitHub is read-only
repository context; its Pages site is a public read-only presentation. Draft state
belongs to local SQLite.
Only approved, non-demo packs may enter `content/approved/`.
